# Puerto del servidor Express (por defecto: 5001)
PORT=5001

# Dirección de escucha (por defecto: 0.0.0.0 para aceptar conexiones del proxy externo)
HOST=0.0.0.0

# Contraseña del panel de administración — REQUERIDA para subir archivos
ADMIN_PASSWORD=cambia_esta_contrasena

# Secreto para firmar las cookies de sesión — REQUERIDO en producción
# Genera uno con: node -e "require('crypto').randomBytes(32).toString('hex') |> console.log"
SESSION_SECRET=genera_un_secreto_largo_y_aleatorio

# Entorno (en producción activa las cookies seguras por HTTPS)
NODE_ENV=production
